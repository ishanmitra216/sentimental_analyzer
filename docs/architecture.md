# System Architecture

Frontend (React Dashboard)
        ↓
Backend API (FastAPI/Flask)
        ↓
ML Layer (Sentiment Model)
        ↓
Data Storage (CSV / JSON)

Key Features:
- Text + CSV Input
- Sentiment Classification
- Real-time Dashboard Visualization